<div class="card col-sm-5 mx-auto mt-5">
  <div class="card-body">
    <h3 class="mt-1 mb-3">新規登録</h3>
    <p class="small form-text">学習積み上げアプリ積読Watcherへようこそ！</p>
    <%= form_with model: @user, url: user_registration_path, id: 'new_user', class: 'new_user', local: true do |f| %>
      <div class="col12">
        <%= devise_error_messages! %>
      </div>

      <div class="form-group">
        <%= f.label :nickname, "ニックネーム", :class => "form-label" %><em>（10文字以内）</em> <br />
        <%= f.text_field :nickname, autofocus: true, :class => "form-controll col-8" %>
      </div>

      <div class="form-group">
        <%= f.label :email, "メールアドレス", :class => "form-label" %><br />
        <%= f.email_field :email, :class => "form-controll col-8" %>
      </div>

      <div class="form-group">
        <%= f.label :password, "パスワード", :class => "form-label" %>
        <% if @minimum_password_length %>
          <em>(<%= @minimum_password_length %> 文字以上)</em>
        <% end %><br />
        <%= f.password_field :password, autocomplete: "off", :class => "form-controll col-8" %>
      </div>

      <div class="form-group">
        <%= f.label :password_confirmation, "パスワード再入力", :class => "form-label" %><br />
        <%= f.password_field :password_confirmation, autocomplete: "off", :class => "form-controll col-8" %>
      </div>

      <p class="small form-text" >このアプリを使用して目指す長期目標を入力しましょう。半年後、数年後、将来どのような人生にしたいですか？</p>
      <div class="form-group">
        <%= f.label :purpose, "目標・目的", :class => "form-label" %><em>（30文字以内）</em> <br />
        <%= f.text_field :purpose, placeholder: "言語化してみましょう", autofocus: true, :class => "form-controll col-8" %>
      </div>

      <div class="actions">
        <%= f.submit "登録", :class => "btn btn-success btn-block" %>
      </div>
      <div class="mt-2">
        <%= link_to "アカウントをお持ちの方はこちら", new_session_path(resource_name) %>
      </div>
    <% end %>
  </div>
</div>

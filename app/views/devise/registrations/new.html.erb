<div class="card col-sm-5 mx-auto mt-5">
  <div class="card-body">
    <h3 class="mt-1 mb-3">新規登録</h3>
    <%= form_with model: @user, url: user_registration_path, id: 'new_user', class: 'new_user', local: true do |f| %>
      <%= devise_error_messages! %>

      <div class="form-group">
        <%= f.label :nickname, "ニックネーム", :class => "form-label" %> <br />
        <%= f.text_field :nickname, autofocus: true, :class => "form-controll" %>
      </div>

      <div class="form-group">
        <%= f.label :email, "メールアドレス", :class => "form-label" %><br />
        <%= f.email_field :email, :class => "form-controll" %>
      </div>

      <div class="form-group">
        <%= f.label :password, "パスワード", :class => "form-label" %>
        <% if @minimum_password_length %>
          <em>(<%= @minimum_password_length %> characters minimum)</em>
        <% end %><br />
        <%= f.password_field :password, autocomplete: "off", :class => "form-controll" %>
      </div>

      <div class="form-group">
        <%= f.label :password_confirmation, "パスワード再入力", :class => "form-label" %><br />
        <%= f.password_field :password_confirmation, autocomplete: "off", :class => "form-controll" %>
      </div>

      <div class="actions">
        <%= f.submit "登録", :class => "btn btn-success btn-block" %>
      </div>
      <div class="mt-2">
        <%= link_to "アカウントをお持ちの方はこちら", new_session_path(resource_name) %>
      </div>
    <% end %>
  </div>
</div>
